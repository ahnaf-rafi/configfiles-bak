export PAGER=less

export EDITOR=nvim
export VISUAL=nvim
export MANPAGER="nvim +Man!"

source <(fzf --zsh)

# alias ls='eza --color=auto'
alias ll='eza -alhF --color=auto --group-directories-first'
alias cat='bat'
alias drs-mbaw='sudo darwin-rebuild switch --flake .#mb-air-work'
alias emacsn='emacs --init-directory ~/.emacs.d-new/'
alias emacsm='emacs --init-directory ~/.minimal.emacs.d/'

# export FONTCONFIG_FILE=$(nix-build -E 'let pkgs = import <nixpkgs> { }; in pkgs.makeFontsConf { fontDirectories = [ pkgs.freefont_ttf ]; }')

# >>> juliaup initialize >>>

# !! Contents within this block are managed by juliaup !!

path=("$HOME/.juliaup/bin" $path)
export PATH

# <<< juliaup initialize <<<
